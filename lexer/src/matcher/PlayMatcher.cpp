#include "matcher/PlayMatcher.h"

namespace dem {
    namespace lexer {
        std::string dem::lexer::PlayMatcher::match(std::string::iterator begin, std::string::iterator end, std::vector<Token> &tokens) const {
            return "";
        }
    }
}