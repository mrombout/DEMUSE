track 1 {
    // X should count from 1 to 5
	var X = 0;
	while(X < 5) {
		X = X + 1;
		print("X: " + X);
	}

    // A should count from 0 to 9
	for(var i = 0; i < 10; i = i + 1) {
		var A = i;
		print("A: " + A);
	}
}