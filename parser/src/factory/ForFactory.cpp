#include "factory/ForFactory.h"

namespace dem {
    namespace parser {
        dem::parser::For *dem::parser::ForFactory::produce(std::deque <lexer::Token> &tokens) {
            return nullptr;
        }
    }
}
